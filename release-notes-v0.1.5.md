# foggy-ai-analysis v0.1.5

## Runtime Alignment

- Points public onboarding to `foggy-runtime-cli v0.1.10`.
- Keeps Java launcher alignment at `foggy-runtime-launcher-v0.1.2`.
- Uses the formal `foggy-ai-analysis` Skill as the sales-drop demo asset source.

## Skill Install

- Documents released zip installs for `foggy-ai-analysis` and `foggy-semantic-query`.
- Keeps install targets limited to `~/.agents/skills`.
- Documents that `demo sales-drop replay` can omit `--skill-dir` when the Skill is installed under the default agent Skill directory.

## CLI Compatibility

- Aligns with CLI support for Runtime API v1 capability preflight, `groupBy` string-array normalization, `-help` compatibility, and paired Skill install metadata.
